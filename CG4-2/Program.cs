using System;

namespace CG4_2
{
    class Program
    {
        static void Main(string[] args)
        {
            //Create and initialize in a single line an array with the following values: 1, 1, 2, 3, 5, 8.
            //Then loop through the array and print out each value.
            
            //Create and array with integers in single line and initialize the array.
            int[] array = new int[] { 1, 1, 2, 3, 5, 8 };

            //Execute the loop according to the array criteria.
            foreach (int i in array)
            
            //Print the array to the console.
            Console.WriteLine(i);
            Console.ReadLine();





            
        }
    }
}
